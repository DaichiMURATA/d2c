# Visual Regression Baselines

This directory contains the baseline screenshots for visual regression testing.
These files are tracked in Git to ensure consistent comparisons across environments.

To update baselines:
```bash
npm run visual-test -- --block=<block-name> --update-baseline
```

